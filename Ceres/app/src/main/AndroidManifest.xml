<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sysu.ceres">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.EditUserActivity"></activity>
        <activity android:name=".activity.StatisticListActivity"></activity>
        <activity android:name=".activity.DoSurveyActivity" />
        <activity android:name=".activity.CreatSurveyActivity" />
        <activity android:name=".activity.CreateMessageActivity" />
        <activity android:name=".activity.EditTaskActivity" />
        <activity android:name=".activity.MyTaskActivity" />
        <activity android:name=".activity.LoginActivity" />
        <activity
            android:name=".activity.TaskDetailActivity"
            android:label="@string/title_activity_task_detail"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>